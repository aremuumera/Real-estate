{
  "version": 2,
  "builds": [
    {
      "src": "api/**/*.js",
      "use": "@vercel/node",
      "config": { "maxDuration": 20 }
    }
  ],
  "rewrites": [
    { "src": "/(.*)", "dest": "/" }
  ]
}
